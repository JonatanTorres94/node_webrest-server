BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[users_lotery] ADD CONSTRAINT [users_lotery_profile_img_df] DEFAULT 'URL POR DEFAULT' FOR [profile_img];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
